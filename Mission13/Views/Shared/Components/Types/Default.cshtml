@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<string>

<a asp-controller="Home" asp-action="Index" asp-route-teamName=""
   class="btn @(ViewBag.SelectedType == null ? "btn-primary" : "btn-outline-secondary") col-md">Home</a>

@foreach (string t in Model)
    {
        <a asp-controller="Home" asp-action="Index" asp-route-teamName="@t"
           class="btn @(ViewBag.SelectedType == t ? "btn-primary" : "btn-outline-secondary") col-md">@t</a>
    }
